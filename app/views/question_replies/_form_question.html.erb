<%= form_with model: @question, url: create_question_product_path do |f|%>
  <%= f.text_area :content, class: "form-control" %>
  <%= f.hidden_field :product_id, value: product.id %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.submit "Tạo", class: "btn btn-primary btn-sm btn-create-question" %>
<% end %>
